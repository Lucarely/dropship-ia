export const metadata = {
  title: 'Operação Dropshipping IA',
  description: 'Painel administrativo da operação de dropshipping.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
